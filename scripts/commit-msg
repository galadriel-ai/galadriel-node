#!/bin/bash

# Get the commit signature status
if ! git log --pretty='%G?' -n 1 HEAD | grep -q 'G'; then
    echo "Error: Commit is not GPG signed. Please sign your commit."
    exit 1
fi

# If the commit is signed, proceed
exit 0
